import 'package:flutter/material.dart';

class GrammerView extends StatelessWidget {
  const GrammerView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: const Text("Grammer View"));
  }
}
