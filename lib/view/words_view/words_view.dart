import 'package:flutter/material.dart';

class WordsView extends StatelessWidget {
  const WordsView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: const Text("Words View"));
  }
}
